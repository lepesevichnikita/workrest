export const endpoint = {
  token: {
    verify: "token/verify", root: "token"
  }, file: {
    root: "file"
  }, users: {
    root: "users"
  },
  jobs: {
    root: "jobs"
  }
};

export default endpoint;
